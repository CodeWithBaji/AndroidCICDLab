package com.androidcicdlab.app.ui.theme

import androidx.compose.ui.graphics.Color

/** Primary brand color (`#6200EE`). */
val PurplePrimary = Color(0xFF6200EE)

/** Darker primary for status bars, dark surfaces, and gradient anchors. */
val PurpleDark = Color(0xFF3700B3)

/** Light purple used on dark surfaces and as a gradient midpoint. */
val PurpleLight = Color(0xFFBB86FC)

/** Soft wash at the bottom of the light-theme background gradient. */
val PurpleMist = Color(0xFFF3E8FF)

/** Deep background anchor for the dark-theme gradient. */
val PurpleDeep = Color(0xFF1A1028)

/** Elevated dark card surface sitting on [PurpleDeep]. */
val PurpleSurfaceDark = Color(0xFF2A1B45)

/** Light card / scaffold surface. */
val PurpleSurfaceLight = Color(0xFFFAF7FF)

/** Light primary container for chips. */
val PurpleContainerLight = Color(0xFFE8D5FF)

val PurpleGrey80 = Color(0xFFCCC2DC)
val PurpleGrey40 = Color(0xFF625B71)

